package com.github.thelawf.ultratext;

import java.awt.*;
import java.util.HashMap;

public class TextFormatter {

    public static HashMap<String, Font> format = new HashMap<>();
    public static HashMap<String, IElement<? extends Argument>> elements = new HashMap<>();

    public void runFormat() {
        // IfBranch ifBranch = new IfBranch();
        // Conditions conditions = new Conditions(ifBranch);
        // elements.put("if", conditions);

    }
}
